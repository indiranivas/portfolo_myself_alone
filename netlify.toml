[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# Set VITE_API_URL in Netlify env to your Render API URL (see DEPLOY.md)

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
